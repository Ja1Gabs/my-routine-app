/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // use the `dark` class on <html> instead of media query
  content: [
    './index.html',
    './src/**/*.{js,jsx,ts,tsx}'
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
